node_modules/
.env
# any .env variant (e.g. a TextEdit-saved .env.rtf) — these hold private keys
.env.*
!.env.example
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
artifacts/
.DS_Store
# written by deploy/studioDeploy.mjs — holds a throwaway Studio private key
.studio-session.json
# throwaway Bradbury debugging probes
probes/
# deploy artifact, regenerated by lib/buildContract.mjs
build/
# agent-local config: launch.json points at a machine-specific scratchpad path
.claude/
